// Temporarily disabled — getProducts is commented out in homePageService
// import ProductSlider from "./ProductSlider";
// import { homePageService } from "../services/homePageService";
// import type { ProductItem, ApiProduct } from "../types";

interface ProductSliderSectionProps {
  title: string;
  endpoint: string;
}

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export default async function ProductSliderSection(_props: ProductSliderSectionProps) {
  // Temporarily disabled while getProducts is paused
  return null;
}
